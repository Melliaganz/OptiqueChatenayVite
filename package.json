{
  "name": "optiquechatenayfinal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "yarn@4.16.0",
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "preact": "^10.29.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-icons": "^5.6.0",
    "react-router-dom": "^7.17.0"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^25.9.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "sharp": "^0.35.1",
    "svgo": "^4.0.1",
    "terser": "^5.48.0",
    "typescript": "6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-css-injected-by-js": "^5.0.1",
    "vite-plugin-image-optimizer": "^2.0.3"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node -e \"import fs from 'fs'; if (fs.existsSync('serve.json')) { if (!fs.existsSync('dist')) fs.mkdirSync('dist'); fs.copyFileSync('serve.json', 'dist/serve.json'); console.log('serve.json copié'); }\"",
    "preview": "vite preview",
    "clean": "node -e \"import fs from 'fs'; ['node_modules', 'dist', '.yarn/cache'].forEach((p) => fs.rmSync(p, { recursive: true, force: true }))\" && yarn cache clean --all"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
